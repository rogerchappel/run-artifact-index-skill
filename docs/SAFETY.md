# Safety Checklist

- Confirm the scan root is the intended run output directory.
- Review generated paths before pasting markdown into a public issue or PR.
- Use `--include-hidden` only when hidden outputs are expected.
- Prefer `--category package` or `--category report` when publishing a narrow review artifact.
- Treat checksums as integrity hints, not proof that content is safe to disclose.
