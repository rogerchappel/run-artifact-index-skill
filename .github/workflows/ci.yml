name: CI

on:
  push:
    branches: [main, "release-candidate/**", "release-readiness/**"]
  pull_request:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm test
      - run: npm run check
      - run: npm run build
      - run: npm run smoke
      - run: bash scripts/validate.sh
